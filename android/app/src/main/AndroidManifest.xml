<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Used by RecordingDetectedNotifier to escalate the "통화녹음 발견"
         notification to the lock screen via setFullScreenIntent. Granted
         by default on Android <14; on 14+ the user can revoke in Special
         App Access → Full screen notifications. Notification still posts
         either way; FSI just brings the lock-screen UX. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="sms" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:networkSecurityConfig="@xml/network_security_config"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/LaunchTheme"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter android:autoVerify="false">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="youngman" />
        </intent-filter>
      </activity>

      <!-- 사장님 정책 (2026-05-21): 잠금화면 통화 후 모달 보장. Activity API
           인 setShowWhenLocked + setTurnScreenOn 으로 잠금 위에 OverlayService
           띄움. 투명 + 애니메이션 없음 + recents 제외. -->
      <activity
          android:name=".overlay.LockScreenLauncherActivity"
          android:theme="@android:style/Theme.Translucent.NoTitleBar"
          android:excludeFromRecents="true"
          android:noHistory="true"
          android:showOnLockScreen="true"
          android:turnScreenOn="true"
          android:exported="false" />

      <!-- 사장님 정책 (2026-05-21 architecture 재설계 §3): 통화 후 모달 진입점.
           background startService 차단 환경에서 안정적으로 OverlayService 를
           foreground state 에서 시작하기 위한 transparent activity. 잠금화면
           위에도 표시 가능. -->
      <!-- 사장님 정책 (2026-05-22 v19): Android 15 BAL_BLOCK 도미노 fix.
           logcat 확인: PostCallScanService(FGS) 의 startActivity 가
           callerStartMode=MODE_BACKGROUND_ACTIVITY_START_SYSTEM_DEFINED 로
           BAL_BLOCK. 그러나 system (uid=1000) 이 통화 종료 시 자동으로
           android.telecom.action.POST_CALL 을 영맨에게 보내려고 시도하는데
           exported=false 라 result code=-91 로 차단되고 있음.
           POST_CALL action filter + exported=true 면 system 이 직접 launch
           → BAL 무관 (system caller). 매번 안정적. -->
      <activity
          android:name=".overlay.CallPostActivity"
          android:theme="@android:style/Theme.Translucent.NoTitleBar"
          android:excludeFromRecents="true"
          android:showOnLockScreen="true"
          android:turnScreenOn="true"
          android:taskAffinity=""
          android:launchMode="singleTop"
          android:exported="true">
        <intent-filter>
            <action android:name="android.telecom.action.POST_CALL" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent-filter>
      </activity>

      <receiver
          android:name=".telephony.CallStateReceiver"
          android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.PHONE_STATE" />
        </intent-filter>
      </receiver>

      <!-- Optional caller-ID role. Only fires when the user picks Youngman in
           Settings → Default apps → Caller ID & spam. Used to surface the
           in-call customer-info banner before the call is answered. -->
      <service
          android:name=".telephony.YoungmanCallScreeningService"
          android:permission="android.permission.BIND_SCREENING_SERVICE"
          android:exported="true">
        <intent-filter>
            <action android:name="android.telecom.CallScreeningService" />
        </intent-filter>
      </service>

      <service
          android:name=".telephony.PostCallScanService"
          android:foregroundServiceType="dataSync"
          android:exported="false" />

      <service
          android:name=".overlay.OverlayService"
          android:foregroundServiceType="dataSync"
          android:exported="false" />

      <service
          android:name=".overlay.SuccessOverlayService"
          android:exported="false" />

      <service
          android:name=".overlay.ProgressOverlayService"
          android:exported="false" />

      <service
          android:name=".overlay.AutoSubmitService"
          android:foregroundServiceType="dataSync"
          android:exported="false" />
    </application>
</manifest>
